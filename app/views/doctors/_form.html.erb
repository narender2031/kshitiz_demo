<%= form_with(model: doctor, local: true) do |form| %>
  <% if doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
      <% doctor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :doctor_name %>
    <%= form.text_field :doctor_name, id: :doctor_doctor_name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.number_field :phone, id: :doctor_phone %>
  </div>

  <div class="field">
    <%= form.label :specialisation %>
    <%= form.text_field :specialisation, id: :doctor_specialisation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
